Run : sudo python one-router.py
        - takes test cases from routes.txt
        - takes route map configs from rmconfig.txt (which is generated by rmap2config.py which is run by one-router.py)
        - generates generated_commands.txt and uses that
        - outputs out_log_<software>.txt

------------------------------------------------------------------------------


